# Lab 6: Code documentation

Use doxygen to document python code

## How to run document generation

The Doxyfile configuration must be first generated with:

```shell
~$ doxygen -g
```

Run `doxygen` on the config file:

```shell
~$ doxygen Doxyfile
```
